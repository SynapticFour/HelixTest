# Release profile only. Do not set target-cpu / AVX2 / NEON rustflags on the
# conformance runner — CI and developer machines must build without assuming
# x86-64-v3 or a specific ARM core.

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "debuginfo"
