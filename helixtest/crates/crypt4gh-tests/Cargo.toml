[package]
name = "crypt4gh-tests"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Local age encrypt/decrypt checks (not Crypt4GH header format). Ferrum HTTP Crypt4GH lives in helixtest-framework."

[dev-dependencies]
anyhow = { workspace = true }
tokio = { workspace = true }
common = { path = "../common", package = "helixtest-common" }
tempfile = { workspace = true }

[lib]
path = "src/lib.rs"
