[package]
name = "crypt4gh-tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
anyhow = { workspace = true }
tokio = { workspace = true }
common = { path = "../common" }
tempfile = { workspace = true }

[lib]
path = "src/lib.rs"

